name: CI

on:
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["main", "develop"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint (ruff + mypy)
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: astral-sh/setup-uv@e5ac9c8fd4f55c31b55e10e8ca8fe0622d585af5  # v5.4.1
        with:
          python-version: "3.12"
      - name: Install dev dependencies
        run: uv sync --extra dev
      - name: ruff check
        run: uv run ruff check src tests
      - name: ruff format
        run: uv run ruff format --check src tests
      - name: mypy
        run: uv run mypy src

  test:
    name: Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-22.04
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: astral-sh/setup-uv@e5ac9c8fd4f55c31b55e10e8ca8fe0622d585af5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dev dependencies
        run: uv sync --extra dev
      - name: Run tests with coverage
        run: |
          uv run pytest tests/ \
            --cov=src/skatteprogressivitet \
            --cov-report=xml \
            --cov-report=term-missing \
            --cov-fail-under=90 \
            -v
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@1e68e06f1dbfde0e4cefc87efeba9e4643565303  # v5.1.2
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage.xml
          flags: python${{ matrix.python-version }}

  audit:
    name: Security audit
    runs-on: ubuntu-22.04
    needs: lint
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: astral-sh/setup-uv@e5ac9c8fd4f55c31b55e10e8ca8fe0622d585af5
        with:
          python-version: "3.12"
      - name: Install dev dependencies
        run: uv sync --extra dev
      - name: pip-audit
        run: uv run pip-audit --strict
      - name: bandit
        run: uv run bandit -r src -lll

  reuse:
    name: REUSE lint
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: fsfe/reuse-action@bb774aa972c2a89ff34781233d275075cbddf542  # v5.0.0

  build:
    name: Build wheel and sdist
    runs-on: ubuntu-22.04
    needs: [test]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: astral-sh/setup-uv@e5ac9c8fd4f55c31b55e10e8ca8fe0622d585af5
        with:
          python-version: "3.12"
      - name: Build
        run: uv build
      - name: Upload dist
        uses: actions/upload-artifact@65c4c4a1ddee5b72f698fef5eb30d5f5f2a1b6f5  # v4.6.1
        with:
          name: dist
          path: dist/
